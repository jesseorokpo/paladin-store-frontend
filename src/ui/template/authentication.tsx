import { Box, Container, Grid } from "@mantine/core";
import React from "react";

export const AuthenticationTemplate: React.FC<{
  children: React.ReactNode;
}> = ({ children }) => {
  return (
    <Container sx={{}}>
  
    </Container>
  );
};
